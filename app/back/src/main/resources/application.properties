spring.application.name=back
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.datasource.url=${DB_CONN_URL:jdbc:mysql://localhost:3306/manage_kanrensha_v02?user=user_kanrensha&password=k1A2n3r4e5N9s8h7a&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Tokyo&rewriteBatchedStatements=true&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:user_kanrensha}
spring.datasource.password=${DB_PASSWORD:k1A2n3r4e5N9s8h7a}
spring.batch.job.enabled=false
server.port= 6180
# server.forward-headers-strategy=native

# フロントエンドのベースURL
app.frontend.base-url=${APP_FRONTEND_BASE_URL:http://localhost:5173}

# 法人番号検索APIドメイン
app.houjinno.url=${APP_HOUJINNO_URL:http://localhost:7080/4/name?}

# アップロードファイルの保存場所(front-back疎通確認時に作成される)
net.seijishikin.jp.normalize.kanrensha.storage_folder = /home/app/store_storage/kanrensha-v02
net.seijishikin.jp.normalize.kanrensha.uncompress = expands

# フロントのダンプ先
net.seijishikin.jp.normalize.kanrensha.front_dump_folder = /front/public/dump

# JWT設定
jwt.key.publickey=classpath:app.pub
jwt.key.privatekey=classpath:app.key
jwt.expiration.access=86400000
jwt.expiration.refresh=604800000

# メール設定
# 環境変数を用いて設定
app.send.mail.address=${APP_SEND_MAIL_ADDRESS:test@example.com}
spring.mail.host= ${MAIL_HOST:chiwaki2024}
spring.mail.port= ${MAIL_PORT:25}
spring.mail.username=${MAIL_USERNAME:your-email@example.com}
spring.mail.password=${MAIL_PASSWORD:your-password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# SQL loging
#  logging.level.org.hibernate.SQL=debug
#  logging.level.org.hibernate.type=trace
#  logging.level.org.hibernate.orm.jdbc.bind=trace
#  logging.level.org.hibernate.orm.jdbc.extract=trace
#  spring.jpa.properties.hibernate.format_sql=true
#  logging.level.org.springframework.jdbc.datasource.init=DEBUG
